apply plugin: 'java-library'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('gson-2.8.2.jar')
    compile project(path: ':shared')
}

sourceCompatibility = "1.6"
targetCompatibility = "1.6"
